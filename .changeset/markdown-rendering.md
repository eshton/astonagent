---
"@astonagent/ui": minor
---

Richer message rendering. Assistant messages now style GFM tables (in a horizontally-scrollable wrapper), ordered/unordered/task lists, headings, blockquotes, horizontal rules, and images. Links render with accent color and open in a new tab with `rel="noreferrer noopener"`. Adds a reusable `<Markdown>` component export.
